---
phase: 01-full-build
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - react-newsletter/src/components/layout/Navbar.jsx
  - react-newsletter/src/components/layout/Footer.jsx
  - react-newsletter/src/components/article/CategoryPill.jsx
  - react-newsletter/src/components/article/ArticleCard.jsx
  - react-newsletter/src/components/edition/EditionCard.jsx
  - react-newsletter/src/App.jsx
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-03
  - LAYOUT-04
  - LAYOUT-05
  - PAGE-06
  - CONT-01
  - CONT-03
must_haves:
  truths:
    - "Navbar is frosted glass, sticky at the top, with working navigation links"
    - "Footer is dark branded with newsletter info and navigation links"
    - "Category pills display colored dots and text matching category identity"
    - "Article cards have 24px rounded corners, borders, hover lift animation, and show reading time"
    - "Edition cards display month, year, issue number, and article count"
  artifacts:
    - path: "react-newsletter/src/components/layout/Navbar.jsx"
      provides: "Frosted glass sticky navbar"
      exports: ["default"]
      min_lines: 20
    - path: "react-newsletter/src/components/layout/Footer.jsx"
      provides: "Dark branded footer"
      exports: ["default"]
      min_lines: 20
    - path: "react-newsletter/src/components/article/CategoryPill.jsx"
      provides: "Color-coded category badge"
      exports: ["default"]
    - path: "react-newsletter/src/components/article/ArticleCard.jsx"
      provides: "Article card with hover lift"
      exports: ["default"]
    - path: "react-newsletter/src/components/edition/EditionCard.jsx"
      provides: "Edition archive card"
      exports: ["default"]
    - path: "react-newsletter/src/App.jsx"
      provides: "Updated routing with /archive and /edition/:slug"
  key_links:
    - from: "react-newsletter/src/components/article/CategoryPill.jsx"
      to: "react-newsletter/src/utils/colorClasses.js"
      via: "named import"
      pattern: "import.*colorClasses.*from"
    - from: "react-newsletter/src/components/article/ArticleCard.jsx"
      to: "react-newsletter/src/components/article/CategoryPill.jsx"
      via: "default import"
      pattern: "import CategoryPill"
---

<objective>
Build all shared components (Navbar, Footer, CategoryPill, ArticleCard, EditionCard) and update routing to add archive and edition routes.

Purpose: These components are consumed by all three pages. Building them first means pages can compose without duplication.
Output: Five reusable components and updated route definitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-full-build/01-CONTEXT.md
@.planning/phases/01-full-build/01-UI-SPEC.md
@.planning/phases/01-full-build/01-01-SUMMARY.md

@react-newsletter/src/utils/colorClasses.js
@react-newsletter/src/lib/cn.js
@react-newsletter/src/data/index.js
@react-newsletter/src/index.css

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From src/utils/colorClasses.js:
```js
export const colorClasses = {
  infrastructure: { pill: "bg-accent-soft text-accent", dot: "bg-accent", label: "Infrastructure" },
  robotics: { pill: "bg-sky-soft text-sky", dot: "bg-sky", label: "Robotics" },
  tools: { pill: "bg-violet-soft text-violet", dot: "bg-violet", label: "Dev Tools" },
  creative: { pill: "bg-pink-soft text-pink", dot: "bg-pink", label: "Creative AI" },
  industry: { pill: "bg-emerald-soft text-emerald", dot: "bg-emerald", label: "Industry" },
};
```

From src/lib/cn.js:
```js
export function cn(...inputs) { return twMerge(clsx(inputs)); }
```

From src/data/index.js:
```js
export const allEditions = [january2026];
export const latestEdition = allEditions[0];
export function getEdition(slug) { ... }
export const categories = [...];
```

Edition object shape: { slug, month, year, issue, coverImage, articles: [...] }
Article object shape: { id, category, featured, title, excerpt, image, readTime, date, body: [...], sources: [...] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Navbar and Footer layout components</name>
  <files>
    react-newsletter/src/components/layout/Navbar.jsx,
    react-newsletter/src/components/layout/Footer.jsx
  </files>
  <read_first>
    react-newsletter/src/pages/HomePage.jsx,
    .planning/phases/01-full-build/01-UI-SPEC.md
  </read_first>
  <action>
    Create directory `react-newsletter/src/components/layout/`.

    **Navbar.jsx** (per LAYOUT-01, D-01):
    - `export default function Navbar()`
    - Sticky frosted glass: `navbar-frosted sticky top-0 z-50 backdrop-blur-md bg-nav-bg border-b border-border-subtle`
    - Height: `h-16` (64px per UI-SPEC)
    - Inner container: `max-w-[1240px] mx-auto px-6 flex items-center justify-between h-full`
    - Left: Brand text "The Gradient" — `text-[13px] font-extrabold tracking-wider uppercase text-text` using `Link` from react-router-dom pointing to `/`
    - Right: Navigation links — `flex items-center gap-6`
      - "Home" link to `/`
      - "Archive" link to `/archive`
      - Each link: `text-[13px] font-extrabold uppercase tracking-wider text-text-3 hover:text-accent transition-colors duration-200`
    - Icons: import `Zap` from lucide-react for brand icon next to "The Gradient" (orange accent color, size 18)

    **Footer.jsx** (per LAYOUT-05, D-02):
    - `export default function Footer()`
    - Background: `bg-footer-bg` (stone-900 dark)
    - Container: `max-w-[1240px] mx-auto px-6 py-16`
    - Top row: "The Gradient" brand in white (`text-white font-extrabold text-lg`) + tagline "Monthly AI intelligence for builders and explorers." in `text-footer-text`
    - Middle row: Nav links (Home, Archive) in `text-footer-text hover:text-white transition-colors`
    - Bottom row: Copyright "2026 The Gradient. All rights reserved." in `text-text-3 text-sm`
    - Use `Link` from react-router-dom for navigation links
  </action>
  <verify>
    <automated>cd react-newsletter && test -f src/components/layout/Navbar.jsx && test -f src/components/layout/Footer.jsx && grep "backdrop-blur" src/components/layout/Navbar.jsx && grep "bg-footer-bg" src/components/layout/Footer.jsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Navbar.jsx exports default function `Navbar`
    - Navbar.jsx contains `sticky top-0 z-50`
    - Navbar.jsx contains `backdrop-blur-md`
    - Navbar.jsx contains `bg-nav-bg`
    - Navbar.jsx contains `navbar-frosted` class (for Firefox fallback in index.css)
    - Navbar.jsx contains `border-b border-border-subtle`
    - Navbar.jsx contains `max-w-[1240px]`
    - Navbar.jsx contains Link components to `/` and `/archive`
    - Footer.jsx exports default function `Footer`
    - Footer.jsx contains `bg-footer-bg`
    - Footer.jsx contains text "The Gradient"
    - Footer.jsx contains text "Monthly AI intelligence for builders and explorers"
    - Footer.jsx contains Link components
  </acceptance_criteria>
  <done>Navbar is frosted glass sticky with brand and nav links; Footer is dark branded with tagline and links</done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryPill, ArticleCard, EditionCard components and update routing</name>
  <files>
    react-newsletter/src/components/article/CategoryPill.jsx,
    react-newsletter/src/components/article/ArticleCard.jsx,
    react-newsletter/src/components/edition/EditionCard.jsx,
    react-newsletter/src/App.jsx
  </files>
  <read_first>
    react-newsletter/src/pages/HomePage.jsx,
    react-newsletter/src/utils/colorClasses.js,
    react-newsletter/src/App.jsx,
    .planning/phases/01-full-build/01-UI-SPEC.md
  </read_first>
  <action>
    Create directories: `src/components/article/` and `src/components/edition/`.

    **CategoryPill.jsx** (per PAGE-06):
    - `export default function CategoryPill({ categoryId })`
    - Import `colorClasses` from `../../utils/colorClasses`
    - Lookup: `const cat = colorClasses[categoryId]`
    - If no match, return null
    - Render: `<span className={cn("inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-[13px] font-extrabold", cat.pill)}>`
    - Inside: dot `<span className={cn("w-1.5 h-1.5 rounded-full", cat.dot)} />` + label text `{cat.label}`
    - Import `cn` from `../../lib/cn`

    **ArticleCard.jsx** (per LAYOUT-03, LAYOUT-04, CONT-01):
    - `export default function ArticleCard({ article })`
    - Import `motion` from `framer-motion`
    - Import `Link` from `react-router-dom`
    - Import `{ Clock, ChevronRight }` from `lucide-react`
    - Import `CategoryPill` from `./CategoryPill`
    - Outer element: `<motion.article>` with:
      - `whileHover={{ y: -6, boxShadow: "0 20px 40px rgba(0,0,0,0.10)" }}`
      - `transition={{ type: "spring", stiffness: 400, damping: 30 }}`
      - className: `rounded-3xl border border-border bg-card-bg overflow-hidden group` (rounded-3xl = 24px)
    - Image container: `div` with `h-[180px] overflow-hidden` — if `article.image`, render `<img>` with `w-full h-full object-cover group-hover:scale-105 transition-transform duration-500`
    - Content container: `div` with `p-6`
    - Top row: `<CategoryPill categoryId={article.category} />`
    - Title: `<h3 className="text-[24px] font-extrabold leading-tight text-text mt-3 group-hover:text-accent transition-colors duration-300">{article.title}</h3>`
    - Excerpt: `<p className="text-[18px] text-text-2 leading-relaxed mt-2 line-clamp-2">{article.excerpt}</p>`
    - Bottom meta row: `flex items-center justify-between mt-4`
      - Left: `<Clock size={14} className="text-text-3" />` + `<span className="text-[13px] text-text-3 font-extrabold">{article.readTime}</span>`
      - Right: "Read Now" link styled as `text-[13px] font-extrabold text-accent flex items-center gap-1` — wrapping the card as a `<Link to={"/edition/" + editionSlug + "#" + article.id}>` or just visual for now. Use the card wrapper as the link instead: wrap entire card content in `<Link>`.

    NOTE: The card links to the edition page. Since we may not know the edition slug at card level, accept an optional `editionSlug` prop. If not provided, link to `/` (homepage). The page components will pass the correct slug.

    **EditionCard.jsx** (per CONT-03, PAGE-05):
    - `export default function EditionCard({ edition })`
    - Import `motion` from `framer-motion`
    - Import `Link` from `react-router-dom`
    - Import `{ ArrowRight }` from `lucide-react`
    - Outer: `<motion.article>` same hover lift pattern as ArticleCard
    - Card: `rounded-3xl border border-border bg-card-bg overflow-hidden group`
    - Content: `p-6`
    - Edition label: `<h3 className="text-[24px] font-extrabold text-text">{edition.month} {edition.year}</h3>`
    - Meta row: Issue number `Issue #{edition.issue}` + article count `{edition.articles.length} articles` in `text-[13px] text-text-3 font-extrabold`
    - CTA: "View Edition" with `ArrowRight` icon in `text-accent font-extrabold text-[13px]`
    - Wrap in `<Link to={"/edition/" + edition.slug}>`

    **Update App.jsx routing** (lazy-load new pages for now with placeholder):
    - Keep existing imports
    - Add imports for new pages (use `lazy` + `Suspense` or direct imports — direct is simpler for static content):
      ```js
      import EditionPage from "./pages/EditionPage";
      import ArchivePage from "./pages/ArchivePage";
      ```
    - Add routes:
      ```jsx
      <Route path="/edition/:slug" element={<EditionPage />} />
      <Route path="/archive" element={<ArchivePage />} />
      ```
    - Keep the old `/article/:id` route for now (will be removed when EditionPage replaces it)
    - Since EditionPage and ArchivePage don't exist yet, create minimal placeholder files:
      - `src/pages/EditionPage.jsx`: `export default function EditionPage() { return <div>Edition Page</div>; }`
      - `src/pages/ArchivePage.jsx`: `export default function ArchivePage() { return <div>Archive Page</div>; }`
  </action>
  <verify>
    <automated>cd react-newsletter && test -f src/components/article/CategoryPill.jsx && test -f src/components/article/ArticleCard.jsx && test -f src/components/edition/EditionCard.jsx && grep "edition/:slug" src/App.jsx && grep "/archive" src/App.jsx && grep "colorClasses" src/components/article/CategoryPill.jsx && grep "whileHover" src/components/article/ArticleCard.jsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - CategoryPill.jsx imports `colorClasses` from `../../utils/colorClasses`
    - CategoryPill.jsx renders a dot span with `rounded-full` and label text
    - ArticleCard.jsx uses `motion.article` with `whileHover={{ y: -6`
    - ArticleCard.jsx contains `rounded-3xl border border-border bg-card-bg`
    - ArticleCard.jsx renders CategoryPill, title, excerpt, and readTime (Clock icon)
    - ArticleCard.jsx contains `group-hover:text-accent` on title
    - EditionCard.jsx displays `edition.month`, `edition.year`, `edition.issue`, `edition.articles.length`
    - EditionCard.jsx uses same hover lift pattern as ArticleCard
    - App.jsx contains route `/edition/:slug` with EditionPage element
    - App.jsx contains route `/archive` with ArchivePage element
    - Placeholder EditionPage.jsx and ArchivePage.jsx exist in src/pages/
  </acceptance_criteria>
  <done>All 5 shared components created with correct props, styling, and animations. Routes updated with edition and archive paths. Placeholder pages exist for import resolution.</done>
</task>

</tasks>

<verification>
1. `cd react-newsletter && npm run build` completes without errors
2. All 5 component files exist in src/components/ subdirectories
3. App.jsx has 4 routes: `/`, `/article/:id`, `/edition/:slug`, `/archive`
4. CategoryPill correctly maps category IDs to color classes from colorClasses.js
</verification>

<success_criteria>
- Navbar is frosted glass sticky with brand + nav links to Home and Archive
- Footer is dark-themed with tagline and navigation
- CategoryPill renders color-coded badges for all 5 categories
- ArticleCard shows image, category pill, title (hover accent), excerpt, reading time, hover lift animation
- EditionCard shows edition metadata (month, year, issue, article count) with hover lift
- Routes for /archive and /edition/:slug are registered in App.jsx
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-full-build/01-02-SUMMARY.md`
</output>
