---
phase: 01-full-build
plan: 06
type: execute
wave: 4
depends_on: ["01-03", "01-04", "01-05"]
files_modified:
  - react-newsletter/src/pages/ArticlePage.jsx
  - react-newsletter/src/App.jsx
  - react-newsletter/src/data.js
autonomous: false
requirements:
  - LAYOUT-06
  - LAYOUT-07
  - LAYOUT-08
  - RESP-01
must_haves:
  truths:
    - "Old dark-themed ArticlePage.jsx is removed"
    - "Old /article/:id route is removed from App.jsx"
    - "All three pages render correctly: /, /edition/january-2026, /archive"
    - "Navigation between all pages works via navbar and card links"
    - "App builds cleanly with no dead imports"
  artifacts:
    - path: "react-newsletter/src/App.jsx"
      provides: "Clean routing with only new routes"
  key_links:
    - from: "react-newsletter/src/App.jsx"
      to: "react-newsletter/src/pages/HomePage.jsx"
      via: "route element"
      pattern: "element.*HomePage"
    - from: "react-newsletter/src/App.jsx"
      to: "react-newsletter/src/pages/EditionPage.jsx"
      via: "route element"
      pattern: "element.*EditionPage"
    - from: "react-newsletter/src/App.jsx"
      to: "react-newsletter/src/pages/ArchivePage.jsx"
      via: "route element"
      pattern: "element.*ArchivePage"
---

<objective>
Clean up legacy files, remove old routes, and perform a visual verification of all three pages working together.

Purpose: Ensure the complete app is functional end-to-end with no dead code or broken navigation before marking Phase 1 complete.
Output: Clean App.jsx with final routes, legacy files removed, visual sign-off from user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-full-build/01-CONTEXT.md
@.planning/phases/01-full-build/01-03-SUMMARY.md
@.planning/phases/01-full-build/01-04-SUMMARY.md
@.planning/phases/01-full-build/01-05-SUMMARY.md

@react-newsletter/src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy files and clean up routing</name>
  <files>
    react-newsletter/src/App.jsx,
    react-newsletter/src/pages/ArticlePage.jsx,
    react-newsletter/src/data.js
  </files>
  <read_first>
    react-newsletter/src/App.jsx,
    react-newsletter/src/pages/ArticlePage.jsx
  </read_first>
  <action>
    1. **Delete** `react-newsletter/src/pages/ArticlePage.jsx` — the old dark-themed article detail page. Its functionality is replaced by EditionPage with inline articles (per RESEARCH.md Pitfall 8).

    2. **Delete** `react-newsletter/src/data.js` — the old flat data file. Its content has been migrated to `src/data/editions/january-2026.js` and the registry at `src/data/index.js`.

    3. **Delete** `react-newsletter/src/App.css` — unused file (contains only `/* unused */`).

    4. **Rewrite `react-newsletter/src/App.jsx`** to remove old routes and imports:
       ```jsx
       import { Routes, Route } from "react-router-dom";
       import HomePage from "./pages/HomePage";
       import EditionPage from "./pages/EditionPage";
       import ArchivePage from "./pages/ArchivePage";

       export default function App() {
         return (
           <Routes>
             <Route path="/" element={<HomePage />} />
             <Route path="/edition/:slug" element={<EditionPage />} />
             <Route path="/archive" element={<ArchivePage />} />
           </Routes>
         );
       }
       ```
       Three routes only. No `/article/:id` route. No ArticlePage import.

    5. **Verify build**: Run `cd react-newsletter && npm run build` to confirm no broken imports remain.
  </action>
  <verify>
    <automated>cd react-newsletter && test ! -f src/pages/ArticlePage.jsx && test ! -f src/data.js && test ! -f src/App.css && grep -c "ArticlePage" src/App.jsx | grep "0" && grep -c "/article/" src/App.jsx | grep "0" && grep "EditionPage" src/App.jsx && grep "ArchivePage" src/App.jsx && npm run build && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/ArticlePage.jsx does NOT exist (deleted)
    - src/data.js does NOT exist (deleted)
    - src/App.css does NOT exist (deleted)
    - App.jsx does NOT contain "ArticlePage" import
    - App.jsx does NOT contain "/article/" route
    - App.jsx contains exactly 3 routes: `/`, `/edition/:slug`, `/archive`
    - App.jsx imports HomePage, EditionPage, ArchivePage
    - `npm run build` passes with zero errors
  </acceptance_criteria>
  <done>All legacy files removed, routing clean with 3 new routes, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete redesign</name>
  <files>none</files>
  <action>Present the running app to the user for visual verification of all pages and interactions.</action>
  <what-built>
    Complete editorial redesign of The Gradient newsletter:
    1. Home page with hero overlay card, secondary cards, 3-column article grid, newsletter signup
    2. Edition page with all articles inline, sticky sidebar TOC with scroll-spy, smooth scroll navigation
    3. Archive page with edition card grid
    4. Frosted glass navbar, dark branded footer
    5. New light theme (charcoal + electric orange palette)
    6. Scroll-reveal animations, card hover lifts
    7. Responsive layouts (desktop 3-col, tablet 2-col, mobile 1-col)
  </what-built>
  <how-to-verify>
    Start the dev server: `cd react-newsletter && npm run dev`

    1. **Home page** (http://localhost:5173/):
       - Hero section: large overlay card with gradient + white text, 2 secondary cards below
       - Recent articles: 3-column grid of article cards with category pills, reading times
       - Newsletter signup: "Stay ahead of the curve" section with email input + "Subscribe Free" button
       - Navbar: frosted glass, sticky on scroll, "The Gradient" brand, Home + Archive links
       - Footer: dark background, tagline, navigation links

    2. **Edition page** (http://localhost:5173/edition/january-2026):
       - All 9 articles displayed in sequence with full content
       - Sticky sidebar TOC on the right (desktop only)
       - Scroll down: TOC highlights the article currently in viewport
       - Click a TOC entry: page smoothly scrolls to that article
       - Each article: category badge, reading time, image (if any), body text, source links

    3. **Archive page** (http://localhost:5173/archive):
       - Card grid showing January 2026 edition
       - Card shows "January 2026", "Issue #01", "9 articles"
       - Clicking card navigates to edition page

    4. **Navigation flow**:
       - Navbar links work between all pages
       - Card "Read Now" links navigate correctly
       - Back/forward browser navigation works

    5. **Responsive** (resize browser or use DevTools):
       - Desktop: 3-column grids, sidebar visible
       - Tablet: 2-column grids, sidebar hidden
       - Mobile: 1-column, sidebar hidden, no horizontal overflow
  </how-to-verify>
  <verify>User confirms visual design matches expectations</verify>
  <done>User approves the visual design and interaction quality of all three pages</done>
  <resume-signal>Type "approved" or describe issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors or warnings
2. All three pages render at their routes
3. No references to old ArticlePage, data.js, or dark-theme tokens remain
4. User visual verification confirms design matches expectations
</verification>

<success_criteria>
- All legacy dark-themed code removed
- Three clean routes: /, /edition/:slug, /archive
- User confirms visual design matches expectations
- Build passes cleanly
- Phase 1 is functionally complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-full-build/01-06-SUMMARY.md`
</output>
