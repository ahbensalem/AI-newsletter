---
phase: 01-full-build
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - react-newsletter/src/pages/ArchivePage.jsx
autonomous: true
requirements:
  - PAGE-05
must_haves:
  truths:
    - "Archive page lists all editions in a card grid"
    - "Each edition card shows month, year, issue number, and article count"
    - "Cards link to the correct edition page"
    - "Layout adapts from 1 column on mobile to 3 columns on desktop"
  artifacts:
    - path: "react-newsletter/src/pages/ArchivePage.jsx"
      provides: "Archive page with edition card grid"
      exports: ["default"]
      min_lines: 30
  key_links:
    - from: "react-newsletter/src/pages/ArchivePage.jsx"
      to: "react-newsletter/src/data/index.js"
      via: "named import"
      pattern: "import.*allEditions.*from"
    - from: "react-newsletter/src/pages/ArchivePage.jsx"
      to: "react-newsletter/src/components/edition/EditionCard.jsx"
      via: "default import"
      pattern: "import EditionCard"
---

<objective>
Build the Archive Page showing all past editions in a responsive card grid with edition metadata.

Purpose: The archive page gives readers access to all monthly editions at a glance.
Output: Working ArchivePage.jsx consuming EditionCard from Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-full-build/01-CONTEXT.md
@.planning/phases/01-full-build/01-UI-SPEC.md
@.planning/phases/01-full-build/01-01-SUMMARY.md
@.planning/phases/01-full-build/01-02-SUMMARY.md

@react-newsletter/src/data/index.js

<interfaces>
From src/data/index.js:
```js
export const allEditions = [...]; // array of edition objects
```

Edition shape: { slug, month, year, issue, coverImage, articles: [...] }

From src/components/edition/EditionCard.jsx:
```jsx
export default function EditionCard({ edition })
```

From src/components/layout/Navbar.jsx:
```jsx
export default function Navbar()
```

From src/components/layout/Footer.jsx:
```jsx
export default function Footer()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Archive Page with edition card grid</name>
  <files>
    react-newsletter/src/pages/ArchivePage.jsx
  </files>
  <read_first>
    react-newsletter/src/pages/ArchivePage.jsx,
    .planning/phases/01-full-build/01-UI-SPEC.md
  </read_first>
  <action>
    REPLACE the placeholder `ArchivePage.jsx` with the full implementation.

    **Imports:**
    ```js
    import { motion } from "framer-motion";
    import Navbar from "../components/layout/Navbar";
    import Footer from "../components/layout/Footer";
    import EditionCard from "../components/edition/EditionCard";
    import { allEditions } from "../data";
    ```

    **Layout** (per PAGE-05, UI-SPEC Layout Grid Archive):
    ```jsx
    export default function ArchivePage() {
      return (
        <div className="min-h-screen bg-page-bg">
          <Navbar />
          <div className="bg-page-bg py-[100px]">
            <div className="max-w-[1240px] mx-auto px-6">
              {/* Page header */}
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5 }}
              >
                <h1 className="text-[40px] font-extrabold text-text">Archive</h1>
                <p className="text-[18px] text-text-2 mt-2">Browse all past editions of The Gradient.</p>
              </motion.div>

              {/* Edition card grid */}
              {allEditions.length === 0 ? (
                <div className="text-center py-16">
                  <p className="text-[18px] text-text-2">The archive is empty.</p>
                  <p className="text-[14px] text-text-3 mt-2">The first edition is coming soon.</p>
                </div>
              ) : (
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 mt-16">
                  {allEditions.map((edition, i) => (
                    <motion.div
                      key={edition.slug}
                      initial={{ opacity: 0, y: 16 }}
                      whileInView={{ opacity: 1, y: 0 }}
                      viewport={{ once: true }}
                      transition={{ duration: 0.4, delay: i * 0.08 }}
                    >
                      <EditionCard edition={edition} />
                    </motion.div>
                  ))}
                </div>
              )}
            </div>
          </div>
          <Footer />
        </div>
      );
    }
    ```

    Key details:
    - Empty state copy: "The archive is empty." / "The first edition is coming soon." (per UI-SPEC copywriting contract)
    - Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8` (same as home page pattern)
    - Section padding: `py-[100px]` per LAYOUT-07
    - Title gap to grid: `mt-16` (64px per LAYOUT-07)
    - Scroll-reveal stagger on cards: `delay: i * 0.08`
    - Page header uses `animate` (above fold, not whileInView)
  </action>
  <verify>
    <automated>cd react-newsletter && grep "allEditions" src/pages/ArchivePage.jsx && grep "import EditionCard" src/pages/ArchivePage.jsx && grep "import Navbar" src/pages/ArchivePage.jsx && grep "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3" src/pages/ArchivePage.jsx && grep "The archive is empty" src/pages/ArchivePage.jsx && npm run build && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ArchivePage.jsx imports `allEditions` from `../data`
    - ArchivePage.jsx imports and renders Navbar and Footer
    - ArchivePage.jsx imports and renders EditionCard for each edition
    - ArchivePage.jsx contains grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8`
    - ArchivePage.jsx contains empty state text "The archive is empty."
    - ArchivePage.jsx contains `py-[100px]` section padding
    - ArchivePage.jsx contains `mt-16` gap between title and grid
    - ArchivePage.jsx uses scroll-reveal animation with `whileInView` on cards
    - ArchivePage.jsx uses `animate` (not whileInView) on page header
    - `npm run build` passes
  </acceptance_criteria>
  <done>Archive page displays all editions in responsive grid with metadata cards, empty state handling, scroll-reveal animations</done>
</task>

</tasks>

<verification>
1. `cd react-newsletter && npm run build` passes
2. ArchivePage renders at `/archive` with edition cards
3. Cards show month, year, issue number, article count
4. Grid is responsive: 1 col mobile, 2 col tablet, 3 col desktop
5. Empty state renders correctly when no editions exist
</verification>

<success_criteria>
- Archive page shows all editions in a card grid
- Each card displays edition metadata (month, year, issue, article count)
- Cards link to /edition/:slug
- Responsive grid layout
- Empty state handled gracefully
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-full-build/01-05-SUMMARY.md`
</output>
