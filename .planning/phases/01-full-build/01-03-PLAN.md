---
phase: 01-full-build
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - react-newsletter/src/pages/HomePage.jsx
autonomous: true
requirements:
  - LAYOUT-02
  - LAYOUT-06
  - LAYOUT-07
  - LAYOUT-08
  - PAGE-01
  - RESP-01
must_haves:
  truths:
    - "Home page shows a hero section with a featured overlay card and 2 secondary cards"
    - "Home page displays a 3-column grid of recent article cards below the hero"
    - "Home page has a newsletter signup placeholder section"
    - "Sections alternate between white and stone-50 backgrounds"
    - "Cards and sections animate on scroll (fade-up reveal)"
    - "Layout adapts from 1 column on mobile to 3 columns on desktop"
  artifacts:
    - path: "react-newsletter/src/pages/HomePage.jsx"
      provides: "Complete home page with hero, article grid, newsletter signup"
      exports: ["default"]
      min_lines: 100
  key_links:
    - from: "react-newsletter/src/pages/HomePage.jsx"
      to: "react-newsletter/src/components/layout/Navbar.jsx"
      via: "default import"
      pattern: "import Navbar"
    - from: "react-newsletter/src/pages/HomePage.jsx"
      to: "react-newsletter/src/components/article/ArticleCard.jsx"
      via: "default import"
      pattern: "import ArticleCard"
    - from: "react-newsletter/src/pages/HomePage.jsx"
      to: "react-newsletter/src/data/index.js"
      via: "named import"
      pattern: "import.*latestEdition.*from"
---

<objective>
Rewrite the Home Page with the new editorial design: hero overlay card with secondary cards, 3-column recent articles grid, newsletter signup placeholder, alternating section backgrounds, and scroll-reveal animations.

Purpose: The home page is the reader's first impression — it must showcase the featured article prominently and provide easy access to recent content.
Output: Fully styled, responsive HomePage.jsx consuming shared components from Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-full-build/01-CONTEXT.md
@.planning/phases/01-full-build/01-UI-SPEC.md
@.planning/phases/01-full-build/01-01-SUMMARY.md
@.planning/phases/01-full-build/01-02-SUMMARY.md

@react-newsletter/src/data/index.js
@react-newsletter/src/index.css

<interfaces>
<!-- From Plan 01 and 02 outputs -->

From src/data/index.js:
```js
export const allEditions = [...];
export const latestEdition = allEditions[0];
export function getEdition(slug) { ... }
export const categories = [...];
```

Edition shape: { slug, month, year, issue, coverImage, articles: [...] }
Article shape: { id, category, featured, title, excerpt, image, readTime, date, body, sources }

From src/components/layout/Navbar.jsx:
```jsx
export default function Navbar() // no props
```

From src/components/layout/Footer.jsx:
```jsx
export default function Footer() // no props
```

From src/components/article/ArticleCard.jsx:
```jsx
export default function ArticleCard({ article, editionSlug }) // editionSlug optional
```

From src/components/article/CategoryPill.jsx:
```jsx
export default function CategoryPill({ categoryId })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build HeroSection with featured overlay card and secondary cards</name>
  <files>
    react-newsletter/src/pages/HomePage.jsx
  </files>
  <read_first>
    react-newsletter/src/pages/HomePage.jsx,
    .planning/phases/01-full-build/01-UI-SPEC.md,
    react-newsletter/src/data/index.js
  </read_first>
  <action>
    COMPLETE REWRITE of `react-newsletter/src/pages/HomePage.jsx`. Delete all existing content and build fresh.

    **Imports:**
    ```js
    import { motion } from "framer-motion";
    import { Link } from "react-router-dom";
    import { ArrowRight, ChevronRight, Clock, Mail } from "lucide-react";
    import Navbar from "../components/layout/Navbar";
    import Footer from "../components/layout/Footer";
    import ArticleCard from "../components/article/ArticleCard";
    import CategoryPill from "../components/article/CategoryPill";
    import { latestEdition, categories } from "../data";
    ```

    **File-private sub-components (NOT exported):**

    1. `HeroSection` — per D-04:
       - Container: `max-w-[1240px] mx-auto px-6 py-[100px]` with `bg-page-bg`
       - Find the featured article: `const featured = latestEdition.articles.find(a => a.featured)`
       - Find 2 secondary articles: `const secondary = latestEdition.articles.filter(a => !a.featured).slice(0, 2)`
       - Featured overlay card: Full-width `relative rounded-3xl overflow-hidden min-h-[400px] lg:min-h-[500px]`
         - Background image: `<img src={featured.image} className="absolute inset-0 w-full h-full object-cover" />`
         - Gradient overlay: `<div className="absolute inset-0 bg-gradient-to-t from-stone-900/90 via-stone-900/40 to-transparent" />`
         - Content positioned bottom-left: `absolute bottom-0 left-0 p-8 lg:p-12 z-10`
         - CategoryPill, then title in `text-white text-[28px] lg:text-[40px] font-extrabold leading-tight mt-3`
         - Excerpt in `text-white/80 text-[18px] mt-3 max-w-xl`
         - "Read Now" CTA: `text-accent text-[13px] font-extrabold flex items-center gap-1 mt-4` with ChevronRight icon
         - Link wraps the card to `/edition/${latestEdition.slug}`
       - Framer Motion: `initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.5 }}`
       - Two secondary cards below: `grid grid-cols-1 sm:grid-cols-2 gap-8 mt-8`
         - Each: `relative rounded-3xl overflow-hidden h-[260px] lg:h-[300px] group`
         - Same gradient overlay pattern but smaller
         - CategoryPill + title in white + "Read Now" link
         - Framer Motion stagger: `initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.4, delay: 0.2 + i * 0.1 }}`

    2. `RecentArticlesGrid` — per D-05:
       - Container: `bg-section-alt py-[100px]` (alternating white section per LAYOUT-06)
       - Inner: `max-w-[1240px] mx-auto px-6`
       - Section title: "Latest Stories" — `text-[24px] font-extrabold text-text`
       - Subtitle: `text-[18px] text-text-2 mt-2`
       - Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 mt-16` (64px gap = mt-16 per LAYOUT-07)
       - Map `latestEdition.articles.filter(a => !a.featured).slice(0, 6)` to `<ArticleCard article={article} editionSlug={latestEdition.slug} />`
       - Scroll-reveal: each card wrapped in `motion.div` with `initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.4, delay: i * 0.08 }}`
       - "View all articles" link at bottom: `text-accent font-extrabold text-[13px] flex items-center gap-1 mt-8` with ArrowRight, linking to `/edition/${latestEdition.slug}`

    3. `NewsletterSignup` — per D-06:
       - Container: `bg-page-bg py-[100px]` (alternating back to stone-50)
       - Inner: `max-w-[600px] mx-auto px-6 text-center`
       - Mail icon: `<Mail size={40} className="text-accent mx-auto" />`
       - Heading: "Stay ahead of the curve" — `text-[24px] font-extrabold text-text mt-6`
       - Body: "One email per month. No spam. The most important AI developments, curated for builders and explorers." — `text-[18px] text-text-2 mt-3`
       - Form (visual only, not functional): `flex gap-3 mt-8 max-w-md mx-auto`
         - Email input: `flex-1 rounded-xl border border-border bg-card-bg px-4 py-3 text-text placeholder:text-text-3` with placeholder "Enter your email"
         - Subscribe button: `bg-accent text-white font-extrabold text-[13px] rounded-xl px-6 py-3 hover:bg-accent/90 transition-colors`
         - Text: "Subscribe Free"

    **Main export:**
    ```jsx
    export default function HomePage() {
      return (
        <div className="min-h-screen bg-page-bg">
          <Navbar />
          <HeroSection />
          <RecentArticlesGrid />
          <NewsletterSignup />
          <Footer />
        </div>
      );
    }
    ```

    Use section separator comments per convention: `/* ═══════════════ Section Name ═══════════════ */`
  </action>
  <verify>
    <automated>cd react-newsletter && grep "HeroSection" src/pages/HomePage.jsx && grep "RecentArticlesGrid" src/pages/HomePage.jsx && grep "NewsletterSignup" src/pages/HomePage.jsx && grep "import Navbar" src/pages/HomePage.jsx && grep "import Footer" src/pages/HomePage.jsx && grep "import ArticleCard" src/pages/HomePage.jsx && grep "bg-section-alt" src/pages/HomePage.jsx && grep "py-\[100px\]" src/pages/HomePage.jsx && grep "whileInView" src/pages/HomePage.jsx && npm run build && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - HomePage.jsx imports Navbar, Footer, ArticleCard, CategoryPill from shared components
    - HomePage.jsx imports latestEdition from "../data"
    - HomePage contains HeroSection with featured overlay card (bg-gradient-to-t, text-white)
    - HomePage contains 2 secondary cards in `grid-cols-2` layout
    - HomePage contains RecentArticlesGrid with `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
    - HomePage contains NewsletterSignup with "Stay ahead of the curve" heading and "Subscribe Free" button
    - Alternating section backgrounds: `bg-page-bg` and `bg-section-alt` used
    - Section padding uses `py-[100px]` per LAYOUT-07
    - Scroll-reveal animations use `whileInView` with `viewport={{ once: true }}`
    - Hero animations use `animate` (not whileInView — above fold)
    - `npm run build` passes
  </acceptance_criteria>
  <done>Home page fully redesigned with hero overlay, article grid, newsletter signup, alternating backgrounds, scroll-reveal animations, responsive layout</done>
</task>

</tasks>

<verification>
1. `cd react-newsletter && npm run build` passes
2. HomePage.jsx is a complete rewrite — no references to old dark-theme colors
3. All three sub-sections render: HeroSection, RecentArticlesGrid, NewsletterSignup
4. Navbar and Footer bookend the page
5. Responsive grid: 1 col mobile, 2 col tablet, 3 col desktop
</verification>

<success_criteria>
- Hero section shows featured article as overlay card with gradient + white text + secondary side-by-side cards
- Recent articles grid displays up to 6 non-featured articles in responsive 3-column layout
- Newsletter signup section is visually complete with email input and subscribe button (non-functional)
- Sections alternate white/stone-50 backgrounds
- Scroll-reveal animations fire on all cards and sections
- Page is responsive across mobile/tablet/desktop breakpoints
- npm run build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-full-build/01-03-SUMMARY.md`
</output>
